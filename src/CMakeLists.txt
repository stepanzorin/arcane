add_subdirectory(util)

target_sources(
    arcane
    PRIVATE # cmake-format: sort
            app_config.hpp
            main.cpp)
            main.cpp
            window_config.cpp
            window_config.hpp)
